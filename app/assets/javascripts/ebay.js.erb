

// response = HTTParty.get("http://open.api.ebay.com/shopping?callname=FindPopularItems&appid=<%= ENV['EBAY_KEY']%>&version=517&siteid=0&QueryKeywords=Jordan7&responseencoding=JSON")
// puts response.body, response.code, response.message, response.headers.inspect


 // Didnt allow the request on the server side, so the client request is the only option. (ICEBOX)
